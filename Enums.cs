public enum LightMode
{
    Red,
    Yellow,
    Green,
}

public enum Axes
{
    Vertical,
    Horizontal,
    Undef
}

public enum PedestrianDirections
{
    Forward = 1,
    Backward = -1,
}

public enum Modes
{
    Auto,
    Manual
}

public enum RoadParts
{
    South,
    West,
    North,
    East,
}

public enum CarDirections
{
    OnLeft,
    Straight,
    OnRight,
}